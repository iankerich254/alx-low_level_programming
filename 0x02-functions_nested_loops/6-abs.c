#include "main.h"
/**
 * _abs - function to compute absolute value of a number
 *
 * n: The number tested
 *
 * Return: absolute value returned
 *
 */

int _abs(int n)
{
	/* My code goes here */
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}
